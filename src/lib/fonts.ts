/* ============================================================
   字体配置 - 使用 CSS @import 方式（备用方案）
   Google Fonts 通过 globals.css 中的 @import 加载
   ============================================================ */

export const fonts = {
  heading:   "",
  body:      "",
  cnBody:    "",
  cnHeading: "",
};